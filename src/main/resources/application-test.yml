# ==============================================
# 测试环境配置 - 阿里云服务器部署
# ==============================================
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tv_recruitment?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&autoReconnect=true
    username: root
    password: Tv@recru2026
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
      validation-timeout: 3000

# 文件存储配置
file:
  storage:
    base-path: ${TV_FILES_PATH:/home/tv_recru/tv-files}
    image-path: /images
    video-path: /videos
    poster-path: /posters
    template-path: /templates
    max-size: 209715200

logging:
  level:
    com.tv.recruitment: info
